<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.mathforall">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".specializations.Technological"></activity>
        <activity android:name=".specializations.NaturalScience" />
        <activity android:name=".specializations.MathAndComputerScience" />
        <activity android:name=".studyYear.TwelftYear" />
        <activity android:name=".studyYear.EleventhYear" />
        <activity android:name=".studyYear.TenthYear" />
        <activity android:name=".studyYear.NinthYear" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>